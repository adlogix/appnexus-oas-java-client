package eu.adlogix.appnexus.oas.client.domain.enums;

/**
 * A {@link TargetingCode} has a {@link TargetGroup} to help in categorizing
 * types to simplify code during push and fetch
 */
public enum TargetGroup {
	GENERAL, ZONE, MOBILE, RDB, SEGMENT;
}
